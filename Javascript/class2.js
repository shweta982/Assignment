//2. Perforn sorting of an array in descending order.
const numbers = [5, 2, 9, 1, 5, 6];
const descendingOrder = numbers.sort((a, b) => b - a);
console.log(descendingOrder); 
// Output will be [9, 6, 5, 5, 2, 1]
